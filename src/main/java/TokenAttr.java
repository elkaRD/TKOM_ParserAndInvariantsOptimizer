public enum TokenAttr
{
    SUM_OPERATOR,
    MUL_OPERATOR,
    AND_OPERATOR,
    OR_OPERATOR,
    RELATIONAL_OPERATOR,
    EQUAL_OPERATOR, //TODO: change name because it's the same in TokenType enum

    VAR_TYPE,
    BOOL_VAL,
    NUM_VAL,
    VAR_VAL,

    STATEMENT
}
